// 引入组件
import Cell from './cell'

// 引入配置
import configForCell from './cell/config'

const components = {
  cell: Cell
}

const configs = {
  cell: configForCell
}

export {
  components,
  configs
}
